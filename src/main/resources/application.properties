server.port=8000
spring.application.name=wikimonitor
app.version=@project.version@
spring.threads.virtual.enabled=true
# MariaDB Configuration
spring.datasource.url=jdbc:mariadb://${TOOL_TOOLSDB_HOST}:3306/${TOOL_TOOLSDB_DATABASE}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${TOOL_TOOLSDB_USER}
spring.datasource.password=${TOOL_TOOLSDB_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.url=${spring.datasource.url}
spring.flyway.user=${spring.datasource.username}
spring.flyway.password=${spring.datasource.password}
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.fail-on-missing-locations=true
spring.flyway.locations=classpath:db/migration

# Cache static resources for 30 minutes
spring.web.resources.cache.cachecontrol.max-age=30m
spring.web.resources.cache.cachecontrol.cache-public=true
spring.web.resources.chain.strategy.content.enabled=true
spring.web.resources.chain.strategy.content.paths=/css/**,/js/**,/images/**,/lib/**

# Session timeout
server.servlet.session.timeout=2h

# SSE stream settings
sse.timeout.ms=${SSE_TIMEOUT_MS:1800000}
# Interval between keepalive heartbeats pushed to each connected client.
sse.heartbeat.ms=${SSE_HEARTBEAT_MS:15000}
sse.event-cache.max-size=${SSE_EVENT_CACHE_MAX_SIZE:1000}
sse.redis.key-prefix=${SSE_REDIS_KEY_PREFIX:wikimonitor}

# Redis (Toolforge default; override per-deployment)
spring.data.redis.host=${REDIS_HOST:redis.svc.tools.eqiad1.wikimedia.cloud}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.repositories.enabled=false
